# KNG7: first_cheap_03 — BTC 5m or 15m (BOT_WINDOW_MINUTES); default btc50_1c (1c + BTC<$50 + 70c TP). Copy .env.example -> .env
services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./exports:/app/exports
