# KNG7: BTC + ETH + SOL + XRP late_high_5m GTC limits. Copy .env.example -> .env
services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./exports:/app/exports
      # Writable state (appuser); survives restarts even if host ./exports is root-owned.
      - bot_data:/app/data

volumes:
  bot_data:
