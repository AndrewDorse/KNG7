# KNG7: first_cheap_03 — dual 3c GTC bids (default) + 99c TP when sized. Copy .env.example -> .env
services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./exports:/app/exports
